# DockerfileWeb
FROM python:3.8-slim-buster
WORKDIR /app
COPY app/requirements.txt /app/

RUN apt-get update && \
    apt-get install -y nano libgdal-dev binutils libproj-dev gdal-bin && \
    pip3 install -r requirements.txt

COPY app/ /app/
CMD ["python", "app/app.py"]